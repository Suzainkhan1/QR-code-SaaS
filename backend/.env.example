# PostgreSQL Database Connection String
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/crunchos?schema=public"

# Secret key for JWT signing (Required - server will fail fast if missing)
JWT_SECRET="your_secure_random_jwt_secret_key"

# Web Server Port
PORT=5000
